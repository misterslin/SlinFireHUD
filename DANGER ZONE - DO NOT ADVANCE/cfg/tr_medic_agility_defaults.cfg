// This is the configuration file can be used to tweak the difficultly of the training map
echo "TR_MEDIC_AGILITY -- DEFAULT CONFIG happs"
sv_cheats 1 // needed for ent_fire

// Use these commands in game to configure the difficultly.
// If the values are not changing, you may need to set 
// the alias "load_config" in tr_medic_<map>.cfg

ent_fire item_healthkit* kill // Remove this line if you need more help

ent_fire trigger_hurt_floor AddOutput "Damage 0.5"
// ent_fire trigger_hurt_floor AddOutput "Damage 15"  // Increase the penalty of touching the floor