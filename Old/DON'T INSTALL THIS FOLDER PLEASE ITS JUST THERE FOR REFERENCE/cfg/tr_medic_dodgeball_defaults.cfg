// This is the configuration file can be used to tweak the difficultly of the training map
echo "TR_MEDIC_DODGEBALL -- DEFAULT CONFIG happs"
sv_cheats 1 // needed for ent_fire

// Use these commands in game to configure the difficultly.
// If the values are not changing, you may need to set 
// the alias "load_config" in tr_medic_<map>.cfg


ent_fire train_circle AddOutput "wheels 90"

ent_fire train_rockets AddOutput "Damage 65" // changes
ent_fire round_timer AddTime "300" // add five minutes to round timer
//ent_fire round_timer SetTime "600" // set round timer to 10 mins

// not sure if the commands below work
ent_fire trigger_cp AddOutput "area_time_to_cap 1" 
ent_fire train_circle SetSpeed "35" 